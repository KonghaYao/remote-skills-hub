# Required: Verdaccio read token (generate via `npm login --registry http://localhost:4873`)
# REGISTRY_READ_TOKEN=

# Optional: Image source (defaults to your-org/remote-skills-hub for local dev)
# GH_REPO=your-org/remote-skills-hub

# Optional: Image tag (defaults to latest)
# IMAGE_TAG=v1.0.0
